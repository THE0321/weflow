<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.messanger.mapper.ScheduleMapper">

    <resultMap id="ScheduleMap" type="com.project.messanger.dto.ScheduleDto">
        <id property="scheduleIdx" column="schedule_idx"/>
        <result property="title" column="title"/>
        <result property="description" column="description"/>
        <result property="location" column="location"/>
        <result property="startDate" column="start_date"/>
        <result property="endDate" column="end_date"/>
        <result property="isAllday" column="is_allday"/>
        <result property="approverIdx" column="approver_idx"/>
        <result property="creatorIdx" column="creator_idx"/>
        <result property="createdDate" column="created_date"/>
        <result property="updatedDate" column="updated_date"/>
    </resultMap>

    <resultMap id="ScheduleWithUserMap" type="com.project.messanger.dto.ScheduleWithUserDto">
        <id property="scheduleIdx" column="schedule_idx"/>
        <result property="title" column="title"/>
        <result property="description" column="description"/>
        <result property="location" column="location"/>
        <result property="startDate" column="start_date"/>
        <result property="endDate" column="end_date"/>
        <result property="isAllday" column="is_allday"/>
        <result property="approverIdx" column="approver_idx"/>
        <result property="approverName" column="approver_name"/>
        <result property="creatorIdx" column="creator_idx"/>
        <result property="creatorName" column="creator_name"/>
        <result property="createdDate" column="created_date"/>
        <result property="updatedDate" column="updated_date"/>
    </resultMap>

    <resultMap id="ScheduleAttenderLinkMap" type="com.project.messanger.dto.ScheduleAttenderLinkDto">
        <id property="linkIdx" column="link_idx"/>
        <result property="scheduleIdx" column="schedule_idx"/>
        <result property="userIdx" column="user_idx"/>
        <result property="isAttend" column="is_attend"/>
    </resultMap>

    <resultMap id="ScheduleAttenderLinkWithUserMap" type="com.project.messanger.dto.ScheduleAttenderLinkWithUserDto">
        <id property="linkIdx" column="link_idx"/>
        <result property="scheduleIdx" column="schedule_idx"/>
        <result property="userIdx" column="user_idx"/>
        <result property="email" column="email"/>
        <result property="userName" column="user_name"/>
        <result property="isAttend" column="is_attend"/>
    </resultMap>

    <sql id="sqlScheduleListCondition">
        FROM schedule
        INNER JOIN user AS creator
           ON creator.user_idx = schedule.creator_idx
        LEFT JOIN user AS approver
          ON approver.user_idx = schedule.approver_idx
        <where>
            <if test="title != null">
                AND (UPPER(schedule.title) LIKE CONCAT('%', UPPER(#{title}), '%') OR UPPER(schedule.description) LIKE CONCAT('%', UPPER(#{title}), '%'))
            </if>
            <if test="schedule_date != null">
                AND (schedule.start_date IS NULL OR DATE_FORMAT(schedule.start_date, '%Y-%m-%d') &lt;= #{schedule_date})
                AND (schedule.end_date IS NULL OR DATE_FORMAT(schedule.end_date, '%Y-%m-%d') &gt;= #{schedule_date})
            </if>
            <if test="user_idx != null">
                AND EXISTS (
                    SELECT 1
                    FROM schedule_attender_link
                    WHERE schedule_attender_link.schedule_idx = schedule.schedule_idx
                      AND schedule_attender_link.user_idx = #{user_idx}
                )
            </if>
        </where>
    </sql>

    <select id="getScheduleCount" parameterType="hashmap" resultType="long">
        SELECT COUNT(schedule.schedule_idx)
        <include refid="sqlScheduleListCondition"/>
    </select>

    <select id="getScheduleList" parameterType="hashmap" resultMap="ScheduleWithUserMap">
        SELECT schedule.schedule_idx
             , schedule.title
             , schedule.description
             , schedule.location
             , schedule.start_date
             , schedule.end_date
             , schedule.is_allday
             , schedule.approver_idx
             , approver.user_name AS approver_name
             , schedule.creator_idx
             , creator.user_name AS creator_name
             , schedule.created_date
             , schedule.updated_date
        <include refid="sqlScheduleListCondition"/>
        ORDER BY schedule.start_date ASC, schedule.end_date ASC
        LIMIT ${limit} OFFSET ${offset}
    </select>

    <select id="getScheduleListByMonth" parameterType="hashmap" resultMap="ScheduleMap">
        SELECT schedule_idx
             , title
             , description
             , location
             , start_date
             , end_date
             , is_allday
             , approver_idx
             , creator_idx
             , created_date
             , updated_date
        FROM schedule
        <where>
            <if test="schedule_date != null">
                AND (start_date IS NULL OR DATE_FORMAT(start_date, '%Y-%m') &lt;= #{schedule_date})
                AND (end_date IS NULL OR DATE_FORMAT(end_date, '%Y-%m') &gt;= #{schedule_date})
            </if>
            <if test="user_idx != null">
                AND EXISTS (
                    SELECT 1
                    FROM schedule_attender_link
                    WHERE schedule_idx = schedule_idx
                    AND user_idx = #{user_idx}
                )
            </if>
        </where>
        ORDER BY start_date ASC, end_date ASC
    </select>

    <select id="getScheduleByIdx" parameterType="hashmap" resultMap="ScheduleWithUserMap">
        SELECT schedule.schedule_idx
             , schedule.title
             , schedule.description
             , schedule.location
             , schedule.start_date
             , schedule.end_date
             , schedule.is_allday
             , schedule.approver_idx
             , approver.user_name AS approver_name
             , schedule.creator_idx
             , creator.user_name AS creator_name
             , schedule.created_date
             , schedule.updated_date
        FROM schedule
        INNER JOIN user AS creator
           ON creator.user_idx = schedule.creator_idx
        LEFT JOIN user AS approver
          ON approver.user_idx = schedule.approver_idx
        WHERE schedule.schedule_idx = #{schedule_idx}
        <if test="user_idx != null">
            AND EXISTS (
                SELECT 1
                FROM schedule_attender_link
                WHERE schedule_attender_link.schedule_idx = schedule.schedule_idx
                  AND schedule_attender_link.user_idx = #{user_idx}
            )
        </if>
    </select>

    <insert id="insertSchedule" parameterType="com.project.messanger.dto.ScheduleDto" useGeneratedKeys="true" keyProperty="scheduleIdx" keyColumn="schedule_idx">
        INSERT schedule
        SET title = #{title},
            <if test="description != null"> description = #{description},</if>
            <if test="location != null"> location = #{location},</if>
            <if test="startDate != null"> start_date = #{startDate},</if>
            <if test="endDate != null"> end_date = #{endDate},</if>
            <if test="approverIdx != null and approverIdx != 0"> approver_idx = #{approverIdx},</if>
            <choose>
                <when test="isAllday != null"> is_allday = #{isAllday},</when>
                <otherwise> is_allday = 'N',</otherwise>
            </choose>
            creator_idx = #{creatorIdx},
            created_date = NOW(),
            updated_date = NOW()
    </insert>

    <update id="updateSchedule" parameterType="com.project.messanger.dto.ScheduleDto">
        UPDATE schedule
        SET <if test="title != null"> title = #{title},</if>
            <if test="description != null"> description = #{description},</if>
            <if test="location != null"> location = #{location},</if>
            <if test="startDate != null"> start_date = #{startDate},</if>
            <if test="endDate != null"> end_date = #{endDate},</if>
            <if test="approverIdx != null and approverIdx != 0"> approver_idx = #{approverIdx},</if>
            <if test="isAllday != null"> is_allday = #{isAllday},</if>
            updated_date = NOW()
        WHERE schedule_idx = #{scheduleIdx}
    </update>

    <delete id="deleteSchedule" parameterType="long">
        DELETE FROM schedule
        WHERE schedule_idx = #{scheduleIdx}
    </delete>

    <select id="getScheduleAttenderLinkList" parameterType="long" resultMap="ScheduleAttenderLinkWithUserMap">
        SELECT schedule_attender_link.link_idx
             , schedule_attender_link.schedule_idx
             , schedule_attender_link.user_idx
             , user.email
             , user.user_name
             , schedule_attender_link.is_attend
        FROM schedule_attender_link
        INNER JOIN user
           ON user.user_idx = schedule_attender_link.user_idx
        WHERE schedule_attender_link.schedule_idx = #{scheduleIdx}
    </select>

    <select id="getScheduleAttenderLinkByIdx" parameterType="long" resultMap="ScheduleAttenderLinkMap">
        SELECT link_idx
             , schedule_idx
             , user_idx
             , is_attend
        FROM schedule_attender_link
        WHERE link_idx = #{linkIdx}
    </select>

    <insert id="insertScheduleAttenderLink" parameterType="com.project.messanger.dto.ScheduleAttenderLinkDto" useGeneratedKeys="true" keyProperty="linkIdx" keyColumn="link_idx">
        INSERT INTO schedule_attender_link
        (
            schedule_idx,
            user_idx,
            is_attend
        )
        VALUES
        <foreach collection="list" item="item" separator=",">
        (
            #{item.scheduleIdx},
            #{item.userIdx},
            #{item.isAttend}
        )
        </foreach>
    </insert>

    <update id="updateScheduleAttender" parameterType="com.project.messanger.dto.ScheduleAttenderLinkDto">
        UPDATE schedule_attender_link
        SET is_attend = #{isAttend}
        WHERE link_idx = #{linkIdx}
          AND user_idx = #{userIdx}
    </update>

    <delete id="deleteScheduleAttenderLink" parameterType="hashmap">
        DELETE FROM schedule_attender_link
        WHERE schedule_idx = #{schedule_idx}
          AND user_idx IN <foreach collection="user_idx_list" item="value" open="(" close=")" separator=",">#{value}</foreach>
    </delete>

</mapper>